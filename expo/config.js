export const BASE_URL = 'http://172.20.10.5:5000';
export const API_BASE = `${BASE_URL}/api`;

// Use your machine's local IP address so the mobile Expo app can reach the backend.
// The backend is hosted on MongoDB via the Node/Express server.
// If your backend runs on another host or port, update BASE_URL accordingly.
